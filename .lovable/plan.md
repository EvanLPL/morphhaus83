# Strengthen the map overlay

## Change
- Increase the existing warm brown map overlay from 28% to a more visible level.
- Keep the current `#7a3d1a` colour and preserve readable streets, labels, and controls.
- Check the homepage map visually after the change.

## Technical details
- Adjust only the overlay opacity in the shared map styling, so both map locations remain consistent.
